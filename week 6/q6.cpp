#include <iostream>
using namespace std;

int main() {
    int n;
    cin >> n;

    int x;
    cin >> x;    // Input

    for (int i = 0; i < n; i++) {
        int y = 0;    // Assignment

        for (int j = 0; j < n; j++) {
            if (j < n) { }    // Comparison
        }
    }

    x = x + 1;    // Arithmetic
    cout << x << endl;    // Output
    return 0;
}